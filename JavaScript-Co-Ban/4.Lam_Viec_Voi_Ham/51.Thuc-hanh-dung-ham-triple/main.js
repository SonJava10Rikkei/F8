// Thực hành dùng hàm triple

// Sử dụng hàm triple đã cho, hãy tạo biến result để lưu kết quả của 10 * 3.

// Sau đó, bạn có thể console.log(result)
//  và mở tab Console trong Dev Tool để xem giá trị của result một cách trực quan.

function triple(x) {
  return 3 * x;
}
var result = triple(10);
console.log(result);
