// Thực hành dùng hàm sum

// Sử dụng hàm sum đã cho, hãy tạo biến result để lưu kết quả của phép cộng 2 số là 20 và 10.

// Sau đó, bạn có thể console.log(result) và
// mở tab Console trong Dev Tool để xem giá trị của result một cách trực quan.

function sum(a, b) {
  return a + b;
}
var result = sum(20, 10);
console.log(result);
